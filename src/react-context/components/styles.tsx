import { defaultStyles as TodoInputStyles } from './TodoInput';
import { defaultStyles as TodoStyles } from './TodoItem';

export const theme = {
    Todo: TodoStyles,
    TodoInput: TodoInputStyles,
};
